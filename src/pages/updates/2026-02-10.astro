---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import Section from '../../components/ui/Section.astro';
import Container from '../../components/ui/Container.astro';
import Badge from '../../components/ui/Badge.astro';

const repo = process.env.GITHUB_REPOSITORY?.split('/')[1] ?? '';
const isUserSiteRepo = repo.endsWith('.github.io');
const basePath = process.env.GITHUB_ACTIONS === 'true' ? (isUserSiteRepo ? '/' : `/${repo}/`) : '/';
const withBase = (url: string) => {
  if (!url.startsWith('/')) return url;
  const clean = url.replace(/^\/+/, '');
  return clean ? `${basePath}${clean}` : basePath;
};
---

<BaseLayout
  title="AI 분류 모델 v3 출시 - NOVA 업데이트"
  description="v3 분류 모델 릴리스 노트."
  lang="ko"
>
  <Header slot="header" lang="ko" />

  <Section padding="large">
    <Container narrow>
      <article class="animate-on-scroll">
        <a href={withBase('/updates')} class="inline-flex items-center text-sm text-neutral-400 hover:text-neutral-100 mb-8">
          &larr; 업데이트 목록으로
        </a>
        <div class="flex items-center gap-3 mb-4">
          <time datetime="2026-02-10" class="text-sm text-neutral-400">2026.02.10</time>
          <Badge text="새 기능" variant="accent" />
        </div>
        <h1 class="text-h1 text-neutral-100 mb-6">AI 분류 모델 v3 출시</h1>
        <p class="text-body-lg text-neutral-400 mb-12">
          분류 정확도와 처리 속도가 개선되었으며, 확장된 언어 처리와 엣지 케이스 라우팅이 포함됩니다.
        </p>

        <div class="space-y-8">
          <section>
            <h2 class="text-h3 text-neutral-100 mb-3">주요 변경사항</h2>
            <ul class="list-disc pl-6 space-y-2 text-neutral-300">
              <li>장문 텍스트 분류 정확도 개선.</li>
              <li>대용량 큐를 위한 추론 경로 최적화.</li>
              <li>저신뢰도 엣지 케이스의 폴백 처리 개선.</li>
            </ul>
          </section>

          <section>
            <h2 class="text-h3 text-neutral-100 mb-3">영향 범위</h2>
            <p class="text-body text-neutral-300">
              Team 및 Enterprise 트랙에 우선 적용되며, Individual 플랜에는 단계적으로 롤아웃됩니다.
            </p>
          </section>

          <section>
            <h2 class="text-h3 text-neutral-100 mb-3">다음 단계</h2>
            <p class="text-body text-neutral-300">
              주요 카테고리에서 모델 동작을 검증하고, 워크플로우 정책에서 임계값을 조정하세요.
            </p>
          </section>
        </div>
      </article>
    </Container>
  </Section>

  <Footer slot="footer" />
</BaseLayout>

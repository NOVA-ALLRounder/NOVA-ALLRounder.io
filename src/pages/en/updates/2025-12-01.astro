---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import Header from '../../../components/Header.astro';
import Footer from '../../../components/Footer.astro';
import Section from '../../../components/ui/Section.astro';
import Container from '../../../components/ui/Container.astro';
import Badge from '../../../components/ui/Badge.astro';

const repo = process.env.GITHUB_REPOSITORY?.split('/')[1] ?? '';
const isUserSiteRepo = repo.endsWith('.github.io');
const basePath = process.env.GITHUB_ACTIONS === 'true' ? (isUserSiteRepo ? '/' : `/${repo}/`) : '/';
const withBase = (url: string) => {
  if (!url.startsWith('/')) return url;
  const clean = url.replace(/^\/+/, '');
  return clean ? `${basePath}${clean}` : basePath;
};
---

<BaseLayout
  title="RBAC and Audit Log Foundation Added — AllvIa Updates"
  description="Release notes for basic security and governance rollout."
  lang="en"
>
  <Header slot="header" lang="en" />

  <Section padding="large">
    <Container narrow>
      <article class="animate-on-scroll">
        <a href={withBase('/en/updates')} class="inline-flex items-center text-sm text-neutral-400 hover:text-neutral-100 mb-8">
          &larr; Back to Updates
        </a>
        <div class="flex items-center gap-3 mb-4">
          <time datetime="2025-12-01" class="text-sm text-neutral-400">2025.12.01</time>
          <Badge text="Security" variant="warning" />
        </div>
        <h1 class="text-h1 text-neutral-100 mb-6">RBAC and Audit Log Foundation Added</h1>
        <p class="text-body-lg text-neutral-400 mb-12">
          Release notes for baseline governance capabilities in team-centric deployments.
        </p>

        <div class="space-y-8">
          <section>
            <h2 class="text-h3 text-neutral-100 mb-3">Security Features</h2>
            <ul class="list-disc pl-6 space-y-2 text-neutral-300">
              <li>Baseline structure for Role-Based Access Control (RBAC).</li>
              <li>Foundation for activity log collection and export.</li>
              <li>Admin change tracking view for governance workflows.</li>
            </ul>
          </section>

          <section>
            <h2 class="text-h3 text-neutral-100 mb-3">Adoption Guide</h2>
            <p class="text-body text-neutral-300">
              Start with a limited role model to verify access boundaries before expanding to the entire team.
            </p>
          </section>
        </div>
      </article>
    </Container>
  </Section>

  <Footer slot="footer" lang="en" />
</BaseLayout>
